type QuizPayload = {
  answers: string[];
  questions: string[];
  path: "skin" | "health" | "sport";
};

function buildSkinRoutine(answers: string[]): string {
  const [skinType, concern, age, _currentRoutine, budget] = answers;

  const cleanser =
    skinType === "چرب" || skinType === "مختلط"
      ? "فوم شستشو CeraVe Foaming (پاک‌سازی عمیق بدون تخریب سد پوستی)"
      : "ژل شستشو ملایم Cetaphil (مناسب پوست خشک و حساس)";

  const toner =
    skinType === "چرب" || skinType === "مختلط"
      ? "تونر Niacinamide 10% (کاهش منافذ و تنظیم چربی)"
      : "تونر آبرسان هیالورونیک اسید (آبرسانی فوری)";

  const serum = (() => {
    if (concern === "جوش و آکنه") return "سرم Niacinamide + Zinc (ضدجوش و ضدالتهاب)";
    if (concern === "چین و چروک") return "سرم رتینول ۰.۵٪ CeraVe (ضدچروک و تجدید سلولی)";
    if (concern === "لک و تیرگی") return "سرم ویتامین C 20% The Ordinary (روشن‌کننده و ضدلک)";
    if (concern === "خشکی و پوسته‌پوسته") return "سرم هیالورونیک اسید (آبرسانی عمیق)";
    return "سرم ویتامین C 20% The Ordinary (روشن‌کننده و آنتی‌اکسیدان)";
  })();

  const moisturizer =
    skinType === "خشک"
      ? "کرم مرطوب‌کننده غنی Cetaphil Cream (آبرسانی ۲۴ ساعته)"
      : "کرم مرطوب‌کننده سبک Cetaphil Lotion";

  const nightExtra =
    concern === "چین و چروک" || age === "بالای ۴۰"
      ? "رتینول ۰.۵٪ CeraVe (۲-۳ شب در هفته)"
      : concern === "لک و تیرگی"
      ? "سرم ویتامین C (هر شب)"
      : "مرطوب‌کننده غنی‌تر شبانه";

  const budgetNote =
    budget === "زیر ۲۰۰ هزار"
      ? "💡 نکته بودجه: با اولویت‌دهی به ضدآفتاب و مرطوب‌کننده شروع کن — اینا بیشترین تأثیر رو دارن."
      : budget === "بیشتر از یک میلیون"
      ? "💡 با این بودجه می‌تونی سرم رتینول و ویتامین C رو هر دو داشته باشی."
      : "";

  return `🌿 روتین پوستی شخصی‌سازی‌شده
نوع پوست: ${skinType} | نگرانی اصلی: ${concern}

━━━ روتین صبح ━━━

۱. ${cleanser}
۲. ${toner}
۳. ${serum}
۴. ${moisturizer}
۵. ضدآفتاب SPF50+ Bioderma (آخرین مرحله — حتماً)

━━━ روتین شب ━━━

۱. میسلار واتر Garnier (پاک‌کردن آرایش)
۲. ${cleanser}
۳. ${toner}
۴. ${nightExtra}
۵. ${moisturizer}

━━━ هفته‌ای یه بار ━━━

- ماسک هیالورونیک اسید (آبرسانی فوری)
${skinType === "چرب" ? "- ماسک گِلی برای پاک‌سازی منافذ" : ""}

⚠️ نکات مهم:
- ضدآفتاب رو هر روز، حتی روزهای ابری استفاده کن
- رتینول رو آروم آروم شروع کن (۱ شب در هفته)
- نتیجه رو بعد از ۴-۶ هفته ارزیابی کن
${budgetNote}`;
}

function buildHealthRoutine(answers: string[]): string {
  const [gender, goal, lifestyle, _budget] = answers;

  const core = (() => {
    if (goal === "انرژی و نشاط بیشتر")
      return [
        "ویتامین B کمپلکس — هر صبح با صبحانه (متابولیسم و انرژی)",
        "ویتامین D3 2000 IU — هر صبح (خلق‌وخو و انرژی)",
        "منیزیم ۴۰۰ میلی‌گرم — شب قبل از خواب (ریلکس و کیفیت خواب)",
        "امگا ۳ ۱۲۰۰ میلی‌گرم — با ناهار (مغز و انرژی پایدار)",
      ];
    if (goal === "تقویت سیستم ایمنی")
      return [
        "ویتامین C 1000 میلی‌گرم — هر صبح (آنتی‌اکسیدان قوی)",
        "زینک ۵۰ میلی‌گرم — هر شب با غذا (ایمنی سلولی)",
        "ویتامین D3 2000 IU — هر صبح",
        "امگا ۳ — هر روز با غذا (ضدالتهاب)",
      ];
    if (goal === "بهبود خواب")
      return [
        "منیزیم گلیسینات ۴۰۰ میلی‌گرم — ۳۰ دقیقه قبل خواب",
        "ویتامین D3 — صبح (چرخه خواب-بیداری)",
        "امگا ۳ — با شام (کاهش کورتیزول)",
        "زینک — شب با غذا (تولید ملاتونین)",
      ];
    if (goal === "سلامت پوست و مو")
      return [
        "بیوتین ۱۰۰۰۰ میکروگرم — هر صبح (مو و ناخن)",
        "کلاژن پپتید ۵۰۰۰ میلی‌گرم — صبح ناشتا (پوست)",
        "ویتامین C 1000 میلی‌گرم — صبح (ساخت کلاژن)",
        "امگا ۳ — با غذا (آبرسانی پوست از داخل)",
      ];
    return [
      "مولتی ویتامین — هر صبح با صبحانه",
      "امگا ۳ — با ناهار",
      "ویتامین D3 — هر صبح",
    ];
  })();

  const lifestyleNote = (() => {
    if (lifestyle === "پشت میزنشین")
      return "⚡ به‌خاطر کم‌تحرکی: ویتامین D3 و منیزیم رو جدی بگیر — اکثر پشت‌میزنشین‌ها کمبود دارن.";
    if (lifestyle === "پر از استرس")
      return "⚡ برای استرس: منیزیم + B کمپلکس مهم‌ترین ترکیبت هستن. روزانه مصرف کن.";
    if (lifestyle === "خواب نامنظم")
      return "⚡ برای خواب: منیزیم ۴۰۰ میلی‌گرم قبل خواب + قطع کافئین بعد از ۲ بعدازظهر.";
    return "";
  })();

  const genderNote =
    gender === "زن"
      ? "👩 مکمل اضافه پیشنهادی: آهن ۱۸ میلی‌گرم + فولیک اسید ۴۰۰ میکروگرم (مخصوص خانم‌ها)"
      : "👨 مکمل اضافه پیشنهادی: زینک ۵۰ میلی‌گرم (مهم برای تستوسترون طبیعی)";

  return `💊 روتین سلامتی شخصی‌سازی‌شده
هدف: ${goal} | سبک زندگی: ${lifestyle}

━━━ مکمل‌های روزانه ━━━

${core.map((item, i) => `${i + 1}. ${item}`).join("\n")}

━━━ اضافه پیشنهادی ━━━

${genderNote}

⚠️ نکات مهم:
- مکمل‌های چربی‌محلول (D، امگا ۳) رو حتماً با غذا بخور
- ویتامین C و زینک رو همزمان نخور — جذب هم رو کم می‌کنن
- بعد از ۳ ماه مصرف منظم آزمایش خون بده
- هیچ مکملی جایگزین تغذیه درست نیست
${lifestyleNote}`;
}

function buildSportRoutine(answers: string[]): string {
  const [goal, experience, days, _budget] = answers;

  const supplements = (() => {
    if (goal === "افزایش حجم عضله") {
      return {
        essential: [
          "پروتئین وی Gold Standard ON — بعد تمرین (۳۰ دقیقه Golden Window)",
          "کراتین مونوهیدرات Creapure — ۵ گرم روزانه (هر روز، نه فقط روز تمرین)",
          "کربوهیدرات سریع — بلافاصله بعد تمرین (موز یا آب‌میوه طبیعی)",
        ],
        optional: [
          "گلوتامین ۵ گرم — قبل خواب (ریکاوری شبانه)",
          "ZMA — شب (تستوسترون طبیعی + خواب)",
        ],
      };
    }
    if (goal === "کات و چربی‌سوزی") {
      return {
        essential: [
          "ایزوله پروتئین وی Dymatize ISO100 — بعد تمرین (کم‌کالری، پروتئین بالا)",
          "BCAA 2:1:1 — حین تمرین (جلوگیری از تجزیه عضله)",
          "ال‌کارنیتین مایع — ۳۰ دقیقه قبل تمرین (چربی‌سوزی)",
        ],
        optional: [
          "کافئین ۲۰۰ میلی‌گرم — قبل تمرین (متابولیسم)",
          "CLA — با وعده‌های غذایی",
        ],
      };
    }
    if (goal === "استقامت و تناسب اندام") {
      return {
        essential: [
          "BCAA 2:1:1 MuscleTech — حین تمرین طولانی",
          "مولتی ویتامین ورزشی — هر روز صبح",
          "امگا ۳ — روزانه (کاهش التهاب مفاصل)",
        ],
        optional: [
          "بتا-آلانین — قبل تمرین (تأخیر در خستگی)",
          "الکترولیت — حین تمرین بیشتر از ۶۰ دقیقه",
        ],
      };
    }
    return {
      essential: [
        "BCAA 2:1:1 — حین تمرین",
        "پروتئین وی — بعد تمرین",
        "گلوتامین — قبل خواب",
      ],
      optional: ["امگا ۳ — روزانه", "ویتامین D3 — صبح"],
    };
  })();

  const preWorkout =
    experience === "پیشرفته (بیشتر از ۲ سال)"
      ? "پری‌ورکاوت C4 Original — ۲۰ دقیقه قبل تمرین (برای سشن‌های سنگین)"
      : "قهوه طبیعی یا چای سبز — کافئین طبیعی کافیه برای مبتدی و متوسط";

  const timingNote =
    days === "۵ روز یا بیشتر"
      ? "🔄 با این حجم تمرین: ریکاوری مهم‌ترین چیزه. خواب ۸ ساعت + گلوتامین رو جدی بگیر."
      : days === "۱ تا ۲ روز"
      ? "💡 با ۲ روز تمرین: پروتئین کافی از غذا + کراتین کافیه. نیازی به مکمل‌های پیچیده نیست."
      : "";

  return `💪 روتین ورزشی شخصی‌سازی‌شده
هدف: ${goal} | تجربه: ${experience}

━━━ زمان‌بندی مکمل‌ها ━━━

قبل تمرین (۲۰-۳۰ دقیقه):
- ${preWorkout}

حین تمرین:
- آب کافی (حداقل ۵۰۰ میلی‌لیتر)
${goal === "افزایش حجم عضله" || goal === "کات و چربی‌سوزی" ? "- BCAA اگر تمرین بیشتر از ۶۰ دقیقه باشه" : ""}

بعد تمرین (تا ۳۰ دقیقه):
${supplements.essential.map((s, i) => `${i + 1}. ${s}`).join("\n")}

━━━ مکمل‌های اختیاری ━━━

${supplements.optional.map((s) => `- ${s}`).join("\n")}

⚠️ نکات مهم:
- پروتئین روزانه: ۱.۶ تا ۲.۲ گرم به ازای هر کیلو وزن بدن
- بدون خواب کافی هیچ مکملی کار نمی‌کنه
- کراتین نیازی به Loading Phase نداره — ۵ گرم روزانه کافیه
- قبل از مصرف Pre-workout به مقاومت کافئین توجه کن
${timingNote}`;
}

export async function POST(req: Request) {
  const { answers, path }: QuizPayload = await req.json();

  let routine = "";

  if (path === "skin") {
    routine = buildSkinRoutine(answers);
  } else if (path === "health") {
    routine = buildHealthRoutine(answers);
  } else if (path === "sport") {
    routine = buildSportRoutine(answers);
  } else {
    routine = buildHealthRoutine(answers);
  }

  return Response.json({ routine });
}
